export interface Item {
    id?: number;
    title: string;
    description: string;
    completed: boolean;
}

export interface Items {
    id: number;
    title: string;
    description: string;
    completed: boolean;
}